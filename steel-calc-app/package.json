{
  "name": "steel-weight-calculator",
  "version": "1.0.0",
  "description": "Steel Weight Calculator App — Triveni Enterprises",
  "scripts": {
    "build": "echo 'Static HTML app — no build step needed'",
    "cap:init": "cap init",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
